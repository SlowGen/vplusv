import React from 'react';

const Collection = (props) => {
    const {product} = props;
    return(
        <div>
            <h1>Hello World!</h1>
            {product}
        </div>
    )
}

export default Collection;
